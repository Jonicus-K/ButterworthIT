<nav class="bg-white/95 dark:bg-gray-800/95 backdrop-blur-md sticky top-0 z-40 border-b border-gray-200 dark:border-gray-700 transition-colors">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
            
            <!-- Brand Logo & Name -->
            <div class="flex items-center gap-3">
                <a href="/" class="flex items-center gap-2.5 text-xl font-extrabold tracking-tight text-gray-900 dark:text-white group">
                    <img src="/assets/logo.png" alt="BWTools Logo" class="h-8 w-auto object-contain group-hover:scale-105 transition-transform" onerror="this.classList.add('hidden'); this.nextElementSibling.classList.remove('hidden');">
                    <span class="hidden text-2xl group-hover:scale-110 transition-transform">🛠️</span>
                    <span>BW<span class="text-blue-600 dark:text-blue-400">Tools</span></span>
                </a>
            </div>

            <!-- Desktop Navigation Dropdowns (Automated via Collections) -->
            <div class="hidden md:flex items-center gap-1 lg:gap-2">
                
                <!-- 1. Image Tools Dropdown -->
                <div class="relative group">
                    <button type="button" class="flex items-center gap-1.5 px-3 py-2 rounded-lg text-sm font-semibold text-gray-700 dark:text-gray-200 hover:text-blue-600 dark:hover:text-blue-400 hover:bg-gray-50 dark:hover:bg-gray-700/50 transition-all">
                        <span>🖼️ Image Tools</span>
                        <svg class="w-4 h-4 transition-transform group-hover:rotate-180 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                        </svg>
                    </button>
                    <!-- Seamless Bridge Wrapper (top-full pt-2 eliminates mouseout gap) -->
                    <div class="absolute left-0 top-full pt-2 w-80 hidden group-hover:block transition-all z-50">
                        <div class="bg-white dark:bg-gray-800 rounded-2xl shadow-xl border border-gray-100 dark:border-gray-700 p-2">
                            {% for tool in collections.tools %}
                                {% if tool.data.category == "image" %}
                                <a href="{{ tool.url }}" class="flex items-center gap-3 p-2.5 rounded-xl hover:bg-gray-50 dark:hover:bg-gray-700 transition-all group/item">
                                    <span class="text-xl group-hover/item:scale-110 transition-transform">{{ tool.data.icon or '✨' }}</span>
                                    <div class="overflow-hidden">
                                        <div class="text-sm font-semibold text-gray-900 dark:text-white group-hover/item:text-blue-600 dark:group-hover/item:text-blue-400 transition-colors truncate">
                                            {{ tool.data.title }}
                                        </div>
                                        {% if tool.data.description %}
                                        <div class="text-xs text-gray-500 dark:text-gray-400 truncate max-w-[220px]">
                                            {{ tool.data.description }}
                                        </div>
                                        {% endif %}
                                    </div>
                                </a>
                                {% endif %}
                            {% endfor %}
                        </div>
                    </div>
                </div>

                <!-- 2. PDF & Document Tools Dropdown -->
                <div class="relative group">
                    <button type="button" class="flex items-center gap-1.5 px-3 py-2 rounded-lg text-sm font-semibold text-gray-700 dark:text-gray-200 hover:text-blue-600 dark:hover:text-blue-400 hover:bg-gray-50 dark:hover:bg-gray-700/50 transition-all">
                        <span>📄 PDF & Documents</span>
                        <svg class="w-4 h-4 transition-transform group-hover:rotate-180 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                        </svg>
                    </button>
                    <!-- Seamless Bridge Wrapper -->
                    <div class="absolute left-0 top-full pt-2 w-80 hidden group-hover:block transition-all z-50">
                        <div class="bg-white dark:bg-gray-800 rounded-2xl shadow-xl border border-gray-100 dark:border-gray-700 p-2">
                            {% for tool in collections.tools %}
                                {% if tool.data.category == "pdf" %}
                                <a href="{{ tool.url }}" class="flex items-center gap-3 p-2.5 rounded-xl hover:bg-gray-50 dark:hover:bg-gray-700 transition-all group/item">
                                    <span class="text-xl group-hover/item:scale-110 transition-transform">{{ tool.data.icon or '📄' }}</span>
                                    <div class="overflow-hidden">
                                        <div class="text-sm font-semibold text-gray-900 dark:text-white group-hover/item:text-blue-600 dark:group-hover/item:text-blue-400 transition-colors truncate">
                                            {{ tool.data.title }}
                                        </div>
                                        {% if tool.data.description %}
                                        <div class="text-xs text-gray-500 dark:text-gray-400 truncate max-w-[220px]">
                                            {{ tool.data.description }}
                                        </div>
                                        {% endif %}
                                    </div>
                                </a>
                                {% endif %}
                            {% endfor %}
                        </div>
                    </div>
                </div>

                <!-- 3. Privacy & Quick Utilities Dropdown -->
                <div class="relative group">
                    <button type="button" class="flex items-center gap-1.5 px-3 py-2 rounded-lg text-sm font-semibold text-gray-700 dark:text-gray-200 hover:text-blue-600 dark:hover:text-blue-400 hover:bg-gray-50 dark:hover:bg-gray-700/50 transition-all">
                        <span>🔒 Utilities</span>
                        <svg class="w-4 h-4 transition-transform group-hover:rotate-180 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                        </svg>
                    </button>
                    <!-- Seamless Bridge Wrapper (right-aligned to avoid screen overflow) -->
                    <div class="absolute right-0 top-full pt-2 w-80 hidden group-hover:block transition-all z-50">
                        <div class="bg-white dark:bg-gray-800 rounded-2xl shadow-xl border border-gray-100 dark:border-gray-700 p-2">
                            {% for tool in collections.tools %}
                                {% if tool.data.category == "privacy" %}
                                <a href="{{ tool.url }}" class="flex items-center gap-3 p-2.5 rounded-xl hover:bg-gray-50 dark:hover:bg-gray-700 transition-all group/item">
                                    <span class="text-xl group-hover/item:scale-110 transition-transform">{{ tool.data.icon or '🔒' }}</span>
                                    <div class="overflow-hidden">
                                        <div class="text-sm font-semibold text-gray-900 dark:text-white group-hover/item:text-blue-600 dark:group-hover/item:text-blue-400 transition-colors truncate">
                                            {{ tool.data.title }}
                                        </div>
                                        {% if tool.data.description %}
                                        <div class="text-xs text-gray-500 dark:text-gray-400 truncate max-w-[220px]">
                                            {{ tool.data.description }}
                                        </div>
                                        {% endif %}
                                    </div>
                                </a>
                                {% endif %}
                            {% endfor %}
                        </div>
                    </div>
                </div>

            </div>

            <!-- Right Controls: Buy Me a Tea & Theme Toggle -->
            <div class="flex items-center gap-3">
                <a href="https://ko-fi.com/bwtools" target="_blank" rel="noopener noreferrer" class="hidden sm:inline-flex items-center gap-1.5 bg-rose-50 dark:bg-rose-900/30 hover:bg-rose-100 dark:hover:bg-rose-900/50 text-rose-600 dark:text-rose-400 text-xs font-bold py-2 px-3.5 rounded-xl border border-rose-200 dark:border-rose-800 transition-all shadow-xs">
                    <span>🫖</span> Buy Me a Tea
                </a>

                <!-- Theme Toggle Button -->
                <button id="themeToggleBtn" type="button" aria-label="Toggle Dark Mode" class="p-2 rounded-xl text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors focus:outline-none">
                    <svg id="themeMoonIcon" class="w-5 h-5 hidden dark:block" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
                    </svg>
                    <svg id="themeSunIcon" class="w-5 h-5 block dark:hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
                    </svg>
                </button>

                <!-- Mobile Hamburger Button -->
                <button id="mobileMenuBtn" type="button" aria-label="Open Navigation Drawer" class="md:hidden p-2 rounded-xl text-gray-600 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors">
                    <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                    </svg>
                </button>
            </div>

        </div>
    </div>

    <!-- Mobile Drawer Menu (Automated via Collections) -->
    <div id="mobileDrawer" class="hidden md:hidden bg-white dark:bg-gray-800 border-t border-gray-200 dark:border-gray-700 px-4 pt-3 pb-6 max-h-[calc(100vh-4rem)] overflow-y-auto">
        
        <!-- Mobile Section 1: Image Tools -->
        <div class="mb-4">
            <div class="text-xs font-bold uppercase tracking-wider text-gray-400 dark:text-gray-500 mb-2 px-2">
                🖼️ Image Editing & Optimisation
            </div>
            <div class="space-y-1">
                {% for tool in collections.tools %}
                    {% if tool.data.category == "image" %}
                    <a href="{{ tool.url }}" class="flex items-center gap-3 px-3 py-2 rounded-xl text-sm font-medium text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors">
                        <span>{{ tool.data.icon or '✨' }}</span>
                        <span>{{ tool.data.title }}</span>
                    </a>
                    {% endif %}
                {% endfor %}
            </div>
        </div>

        <!-- Mobile Section 2: PDF & Document Tools -->
        <div class="mb-4">
            <div class="text-xs font-bold uppercase tracking-wider text-gray-400 dark:text-gray-500 mb-2 px-2">
                📄 PDF & Document Tools
            </div>
            <div class="space-y-1">
                {% for tool in collections.tools %}
                    {% if tool.data.category == "pdf" %}
                    <a href="{{ tool.url }}" class="flex items-center gap-3 px-3 py-2 rounded-xl text-sm font-medium text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors">
                        <span>{{ tool.data.icon or '📄' }}</span>
                        <span>{{ tool.data.title }}</span>
                    </a>
                    {% endif %}
                {% endfor %}
            </div>
        </div>

        <!-- Mobile Section 3: Privacy & Quick Utilities -->
        <div class="mb-4">
            <div class="text-xs font-bold uppercase tracking-wider text-gray-400 dark:text-gray-500 mb-2 px-2">
                🔒 Privacy & Quick Utilities
            </div>
            <div class="space-y-1">
                {% for tool in collections.tools %}
                    {% if tool.data.category == "privacy" %}
                    <a href="{{ tool.url }}" class="flex items-center gap-3 px-3 py-2 rounded-xl text-sm font-medium text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors">
                        <span>{{ tool.data.icon or '🔒' }}</span>
                        <span>{{ tool.data.title }}</span>
                    </a>
                    {% endif %}
                {% endfor %}
            </div>
        </div>

        <!-- Mobile Ko-fi Support Link -->
        <div class="pt-4 border-t border-gray-200 dark:border-gray-700">
            <a href="https://ko-fi.com/bwtools" target="_blank" rel="noopener noreferrer" class="flex items-center justify-center gap-2 w-full bg-rose-50 dark:bg-rose-900/30 text-rose-600 dark:text-rose-400 font-bold py-2.5 px-4 rounded-xl border border-rose-200 dark:border-rose-800 text-sm">
                <span>🫖</span> Buy Me a Tea
            </a>
        </div>

    </div>
</nav>

<script>
    // Mobile Drawer Toggle
    const mobileMenuBtn = document.getElementById('mobileMenuBtn');
    const mobileDrawer = document.getElementById('mobileDrawer');
    if (mobileMenuBtn && mobileDrawer) {
        mobileMenuBtn.addEventListener('click', () => {
            mobileDrawer.classList.toggle('hidden');
        });
    }

    // Dark Mode LocalStorage Toggle
    const themeToggleBtn = document.getElementById('themeToggleBtn');
    if (themeToggleBtn) {
        themeToggleBtn.addEventListener('click', () => {
            if (document.documentElement.classList.contains('dark')) {
                document.documentElement.classList.remove('dark');
                localStorage.setItem('theme', 'light');
            } else {
                document.documentElement.classList.add('dark');
                localStorage.setItem('theme', 'dark');
            }
        });
    }
</script>